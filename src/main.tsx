import React from 'react'
import ReactDOM from 'react-dom/client'
import NodesWithConnect from './NodesWithConnect.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <NodesWithConnect />
  </React.StrictMode>,
)
